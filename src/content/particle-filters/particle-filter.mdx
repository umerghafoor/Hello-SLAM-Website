import { Callout } from '@/components/Callout';

<Callout variant="todo">
  Full lesson body coming soon — ported from
  <code> notebooks/2_particle_filters/2_particle_filter.ipynb</code>.
</Callout>

## Monte Carlo Localization

When the belief is too weird for a Gaussian — multimodal, asymmetric, constrained by
walls — we approximate it with a cloud of weighted **particles**. Each tick we sample
from the motion model, weight by the sensor model, and resample.
