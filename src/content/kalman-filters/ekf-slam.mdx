import { Callout } from '@/components/Callout';

<Callout variant="todo">
  Full lesson body coming soon — ported from
  <code> notebooks/1_kalman_filters/4_ekf_slam.ipynb</code>.
</Callout>

## EKF-SLAM

In **EKF-SLAM** we stack the robot pose and every landmark position into a single state
vector and run an EKF over the joint Gaussian. The covariance matrix grows quadratically
with the number of landmarks, which is the dominant practical limit of the approach.
