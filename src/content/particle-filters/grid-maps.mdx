import { Callout } from '@/components/Callout';

<Callout variant="todo">
  Full lesson body coming soon — ported from
  <code> notebooks/2_particle_filters/1_grid_maps.ipynb</code>.
</Callout>

## Occupancy Grid Maps

When the environment can’t be described by a handful of landmarks, we represent it as a
**grid of cells**, each holding the probability that the cell is occupied. We update
each cell independently in **log-odds** form so updates become additive.
